# Test-only dependencies (not installed in the runtime container).
# Install with: pip install -r backend/requirements.txt -r backend/requirements-dev.txt
# Run tests with: pytest tests/test_backend_*.py tests/test_playsight_*.py

pytest==9.0.2
httpx==0.27.2   # required by fastapi.testclient.TestClient
