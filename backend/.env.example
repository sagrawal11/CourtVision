# Supabase Configuration
SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
SUPABASE_ANON_KEY=your_supabase_anon_key

# API Configuration
ALLOWED_ORIGINS=http://localhost:3000
API_PORT=8000

# Environment
ENVIRONMENT=development

# Video Processing
# Free-tier stack: Supabase Storage is used for temp video storage (no AWS needed).
# CV processing (court_setup_job.py) runs locally as a subprocess — no GPU cloud cost.
BACKEND_URL=http://localhost:8000

# Production only — swap in when ready to scale:
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=courtvision-uploads


