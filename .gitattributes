# Model weights and video assets are not committed to git — they are produced
# or downloaded separately and are excluded via .gitignore (*.pt, *.cbm, *.mp4, …).
# If large files ever need to be tracked, configure Git LFS here.
